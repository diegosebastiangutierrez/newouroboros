{#
/**
 */
#}
{{ form.messages }}
<div class="poll">
  <div class="vote-form">
    {% if show_question %}
      <div class="h3 poll-question">{{ question }}</div>
    {% endif %}

    {{ form.choice }}

    {{ form.actions }}
  </div>
{{ form|without('actions', 'choice', 'messages', 'question') }}
</div>
